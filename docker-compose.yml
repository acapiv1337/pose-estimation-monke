services:
  backend:
    build: ./backend
    container_name: fastapi-backend
    env_file: .env
    ports:
      - "8001:8001"
  frontend:
    build: ./frontend
    container_name: vue-frontend
    env_file: .env
    ports:
      - "5173:80"
    depends_on:
      - backend
