services:
  backend:
    build: ./backend
    container_name: fastapi-backend
    ports:
      - "8001:8001"
  frontend:
    build: ./frontend
    container_name: vue-frontend
    ports:
      - "5173:80"
    depends_on:
      - backend
